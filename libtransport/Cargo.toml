[package]
name = "sirfz-transport"
version = "1.0.0"
edition = "2021"

[lib]
name = "sirfz"
crate-type = ["cdylib"]

[dependencies]
hmac = "0.12"
sha2 = "0.10"
zeroize = { version = "1.8", features = ["derive"] }
getrandom = "0.2"

[profile.release]
panic = "abort"
lto = "fat"
codegen-units = 1
strip = "symbols"
overflow-checks = true
opt-level = "z"
